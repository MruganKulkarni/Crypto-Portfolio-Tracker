server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: git@github.com:MruganKulkarni/crypto-config-repo.git
          clone-on-start: true