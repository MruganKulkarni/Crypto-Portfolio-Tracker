spring:
  application:
    name: holding-service
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:mysql://3.109.132.110:3306/holding_db
    username: holding_user
    password: HoldingPass@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8082

eureka:
  client:
    enabled: false